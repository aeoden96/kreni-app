changed_files="$(git diff-tree -r --name-only --no-commit-id ORIG_HEAD HEAD)"

check_run() {
	echo "$changed_files" | grep --quiet "$1" && eval "$2"
}

# Run yarn install if yarn.lock changed
check_run "yarn.lock" "yarn install --prefer-offline"
