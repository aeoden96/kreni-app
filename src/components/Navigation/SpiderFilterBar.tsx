type Props = {
  animationDelay?: number;
  routePath: string;
};

/** Layer toggles for driving/city/cycling live on the map (badges). Kept for API compatibility with SpiderRouteList. */
export function SpiderFilterBar(_props: Props) {
  return null;
}
